<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Subasta <%= subasta.id_subasta %> | <%= subasta.titulo_bien %></title>

  <link rel="stylesheet" href="/css/bootstrap.min.css" />
  <link rel="stylesheet" href="/css/all.min.css" />
  <link rel="stylesheet" href="/css/menu.css" />
</head>

<body data-bs-theme="dark" style="background-color: #121212; color: #f1f1f1;">

  <div class="container py-5">

    <!-- volver -->
    <a href="/menu" class="text-secondary text-decoration-none mb-3 d-inline-block">
      <i class="fa fa-arrow-left me-2"></i> Volver
    </a>

    <!-- tarjeta principal -->
    <div class="card bg-dark border-light p-4">
      <div class="row g-4">

        <!-- Imagen -->
        <div class="col-md-5">
          <img
            src="<%= subasta.imagen_url_bien || 'https://via.placeholder.com/600x400?text=Sin+imagen' %>"
            class="img-fluid rounded border"
            alt="Imagen del bien"
            style="max-width:100%; max-height:300px; object-fit:cover;"
          />
        </div>

        <!-- Info -->
        <div class="col-md-7">

          <h1 class="h4 mb-3 text-white">
            <%= subasta.titulo_bien %>
          </h1>

          <p class="text-muted">
            <%= subasta.descripcion_bien %>
          </p>

          <ul class="list-unstyled small">
            <li><strong>ID Subasta:</strong> <%= subasta.id_subasta %></li>
            <li><strong>ID Bien:</strong> <%= subasta.id_bien %></li>
            <li><strong>Estado actual:</strong> 
              <span class="text-capitalize"><%= subasta.estado %></span>
            </li>
            <li><strong>Precio base:</strong> Gs. <%= subasta.precio_base %></li>
            <li><strong>Valor declarado del bien:</strong> Gs. <%= subasta.valor_inicial_bien %></li>
            <li><strong>Fecha de inicio:</strong> <%= subasta.fecha_inicio %></li>
            <li><strong>Fecha de cierre:</strong> <%= subasta.fecha_fin %></li>
          </ul>

        </div>
      </div>
    </div>

  </div>
</body>
</html>
